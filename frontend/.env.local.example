# URL of the FastAPI backend. For local dev, the default below is fine.
# For Vercel deployment, set this to your hosted backend URL (e.g. ngrok, Railway, Render).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
